---
# tasks file for roles/client main
- name: Ubuntu procedure
  ansible.builtin.include_role:
    name: "{{ kubernetesConfigurations.operatingSystem }}"

- name: Define node as Client
  ansible.builtin.include_role:
    name: kubernetes
    tasks_from: kubernetesClient
